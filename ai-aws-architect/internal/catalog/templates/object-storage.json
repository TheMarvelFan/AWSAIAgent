{
  "id": "object-storage-bucket",
  "name": "Object storage (private S3 bucket)",
  "category": "storage",
  "summary": "A private, versioned, encrypted S3 bucket for application data, uploads or backups. No public access under any parameter combination.",
  "provides": ["object storage", "file uploads", "backups", "data lake landing zone"],
  "resources": [
    "aws_s3_bucket",
    "aws_s3_bucket_versioning",
    "aws_s3_bucket_server_side_encryption_configuration",
    "aws_s3_bucket_public_access_block",
    "aws_s3_bucket_lifecycle_configuration"
  ],
  "parameters": [
    {
      "name": "bucket_name_prefix",
      "type": "string",
      "required": true,
      "pattern": "^[a-z0-9][a-z0-9-]{2,40}$"
    },
    {
      "name": "versioning",
      "type": "bool",
      "required": false,
      "default": true
    },
    {
      "name": "expire_after_days",
      "type": "int",
      "required": false,
      "default": 30,
      "min": 1,
      "max": 365,
      "description": "Lifecycle expiry. Keeps demo data from accumulating cost."
    }
  ],
  "estimated_cost_usd_per_month": { "low": 0.1, "high": 2.0 },
  "notes": "Public read is deliberately not a parameter. Serving files publicly is what the static-site block is for."
}
